#!/usr/bin/env rundown --ask

[](label:all)

# Building Rundown

This script builds rundown into `dist/`

[](label:build)

## Build executable

``` bash named
# Building

if [ -f dist ]; then
  rm -f dist/*
fi

mkdir -p dist

go build -o dist/rundown
cp bash_autocomplete dist
```

Rundown built and available at `dist/rundown`.

[](label:autocomplete)

## Autocomplete

Bash autocomplete can be added via:

``` bash reveal named
# Setup autocomplete
PROG=rundown source dist/bash_autocomplete
```
